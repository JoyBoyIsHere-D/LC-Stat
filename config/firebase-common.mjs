/**
 * Shared Firebase project configuration
 * This file is used to store common Firebase project details that can be shared
 * between frontend and backend, but doesn't contain sensitive information.
 */
export const FIREBASE_CONFIG = {
    projectId: 'lc-stat',
}

export const USERS_COLLECTION = 'users';
